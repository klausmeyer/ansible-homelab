- name: Install baseline packages
  apt:
    name: "{{ item }}"
    state: present
  with_items:
    - apt-transport-https
    - ca-certificates
    - curl
    - git
    - gnupg
    - htop
    - ncdu
    - software-properties-common
    - sudo
    - tig
    - tmux
    - vim
    - wget
